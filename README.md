# Rails API Cheatsheet & Project Structure (Buat Belajar Aja Ges)

## 🔧 Generate

| Command                                                           | Deskripsi                            |
| ----------------------------------------------------------------- | ------------------------------------ |
| `rails new myapp --api -d postgresql`                             | Buat project API-only pakai Postgres |
| `rails g model Task title:string due_date:datetime status:string` | Generate model + migration           |
| `rails g controller api/v1/tasks`                                 | Generate controller                  |
| `rails g job OverdueCheck`                                        | Generate job (Sidekiq/ActiveJob)     |
| `rails g mailer TaskMailer`                                       | Generate mailer                      |
| `rails g channel Task`                                            | Generate ActionCable channel         |

## ⚙️ Database

| Command                    | Description                          |
| -------------------------- | ------------------------------------ |
| `rails db:create`          | Buat database (lihat `database.yml`) |
| `rails db:migrate`         | Jalankan migration                   |
| `rails db:rollback`        | Balik 1 migration                    |
| `rails db:rollback STEP=3` | Balik 3 migration                    |
| `rails db:reset`           | Drop, create & migrate ulang         |
| `rails db`                 | Masuk psql console                   |

## 🏃 Jalankan server

| Command           | Description        |
| ----------------- | ------------------ |
| `rails s`         | Start Rails server |
| `rails s -p 3001` | Ganti port ke 3001 |

## 🔌 Redis & Sidekiq

| Command                                     | Description                        |
| ------------------------------------------- | ---------------------------------- |
| `redis-server`                              | Start Redis server                 |
| `bundle exec sidekiq -C config/sidekiq.yml` | Start Sidekiq worker dengan config |

## 🔥 ActionCable

- Sudah built-in di rails s

- Tes websocket bisa pakai wscat:

```bash
wscat -c ws://localhost:3000/cable
```

## 🔍 Lainnya

| Command                       | Description                            |
| ----------------------------- | -------------------------------------- |
| `rails routes`                | Lihat semua endpoint                   |
| `rails console` / `rails c`   | Masuk IRB console dengan context Rails |
| `reload!`                     | Reload environment di console          |
| `tail -f log/development.log` | Lihat log realtime                     |

## 📦 Bundler

| Command          | Description        |
| ---------------- | ------------------ |
| `bundle install` | Install dependency |
| `bundle update`  | Update dependency  |

## 📁 Struktur Project

```bash
task_api/
├── app/
│   ├── controllers/ # Controller HTTP request
│   ├── jobs/        # Background jobs (Sidekiq/ActiveJob)
│   ├── mailers/     # Kirim email
│   ├── models/      # ORM ActiveRecord (Task < ApplicationRecord)
│   ├── views/       # Untuk email template (misal done_email.html.erb)
│   └── ...
├── config/
│   ├── routes.rb      # Semua route API
│   ├── cable.yml      # Config ActionCable (websocket)
│   ├── database.yml   # Config database
│   ├── environments/  # Setting per environment
│   └── initializers/  # Load script startup (sidekiq.rb, redis.rb, mailer.rb)
├── db/
│   ├── migrate/       # File migration schema
│   ├── schema.rb      # Snapshot struktur database
├── log/               # Log development & production
├── public/            # File static (jarang terpakai di API-only)
├── Gemfile            # Dependency Ruby Gem (seperti package.json)
├── Rakefile           # Task CLI Rails
└── README.md
```

### 📌 Summary

- `app/controllers` ➔ handle HTTP request, return JSON

- `app/models` ➔ mapping ke database table

- `app/jobs` ➔ background task (Sidekiq)

- `app/mailers` ➔ kirim email

- `app/channels` ➔ realtime websocket

- `app/views` ➔ biasanya hanya untuk template email (karena API)

- `config/` ➔ konfigurasi routes, environment, sidekiq, redis, mailer

- `db/` ➔ migration & schema database

- `log/` ➔ file log

### IMPORTANT:

This README is generated by AI.